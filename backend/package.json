{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for ImgToFlex Ascend",
  "main": "app.js",
  "scripts": {
    "test": "test",
    "dev": "nodemon app.js",
    "build:ui": "rm -rf dist && cd ../frontend/ && npm run build && cp -r dist ../backend && echo 'rebuilt UI in Linux'",
    "windows:build:ui": "wsl rm -rf dist && cd ../frontend/ && npm run build && wsl cp -r dist ../backend && wsl echo 'rebuilt UI in Windows'"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.651.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mongoose": "^8.6.1",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^3.1.4"
  }
}
